{
  "name": "landing-page-builder",
  "version": "1.0.0",
  "private": true,
  "description": "AI landing page builder -- Express + MongoDB backend, React + Vite frontend",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm start --prefix backend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "smoke": "npm run smoke --prefix backend",
    "setup": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.2"
  }
}

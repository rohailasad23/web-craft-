{
  "name": "web-craft",
  "version": "1.0.0",
  "private": true,
  "description": "web craft -- discover, preview and download ready-made website templates",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm start --prefix backend",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "smoke": "npm run smoke --prefix backend",
    "setup": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.2"
  }
}
